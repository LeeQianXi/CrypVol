name: test-dispatch.yml
on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（留空则自动使用日期格式）'
        required: false
        default: ''
      prerelease:
        description: '标记为预发布'
        type: boolean
        default: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Workflow dispatch works!"
